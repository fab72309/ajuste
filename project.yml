name: BioTrack
options:
  bundleIdPrefix: com.fabienlopes
  deploymentTarget:
    iOS: "15.0"
settings:
  base:
    SWIFT_VERSION: 5.9
    ENABLE_USER_SCRIPT_SANDBOXING: NO
targets:
  BioTrack:
    type: application
    platform: iOS
    deploymentTarget: "15.0"
    sources:
      - path: BioTrack
        excludes:
          - PrivacyPolicy.md
    info:
      path: BioTrack/Info.plist
      properties:
        CFBundleDisplayName: AJUSTE
        CFBundleShortVersionString: $(MARKETING_VERSION)
        CFBundleVersion: $(CURRENT_PROJECT_VERSION)
        UILaunchScreen:
          UIColorName: Background
        UISupportedInterfaceOrientations:
          - UIInterfaceOrientationPortrait
          - UIInterfaceOrientationPortraitUpsideDown
        UIUserInterfaceStyle: Automatic
        NSSupportsLiveActivities: true
        NSSupportsLiveActivitiesFrequentUpdates: true
        ITSAppUsesNonExemptEncryption: false
        NSHealthShareUsageDescription: "AJUSTE lit les données Santé que vous choisissez (sommeil, pas, poids, fréquence cardiaque au repos, HRV) afin de pré-remplir vos métriques localement."
        NSHealthUpdateUsageDescription: "AJUSTE ne modifie aucune donnée Santé. Cette déclaration est requise par HealthKit ; l’app utilise Santé uniquement en lecture, avec votre accord."
        CFBundleURLTypes:
          - CFBundleURLName: com.fabienlopes.biotrack
            CFBundleURLSchemes:
              - biotrack
    entitlements:
      path: BioTrack/BioTrack.entitlements
      properties:
        com.apple.developer.healthkit: true
        com.apple.security.application-groups:
          - group.com.fabienlopes.biotrack
    dependencies:
      - target: BioTrackWidgetExtension
        embed: true
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.fabienlopes.biotrack
        CODE_SIGN_STYLE: Automatic
        DEVELOPMENT_TEAM: X4Z4PH88ZT
        CURRENT_PROJECT_VERSION: 14
        MARKETING_VERSION: 1.2.4
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        TARGETED_DEVICE_FAMILY: "1"
  BioTrackWidgetExtension:
    type: app-extension
    platform: iOS
    deploymentTarget: "17.0"
    sources:
      - path: BioTrackWidgetExtension
      - path: BioTrack/Models
      - path: BioTrack/Shared
    info:
      path: BioTrackWidgetExtension/Info.plist
      properties:
        CFBundleDisplayName: AJUSTE Widget
        CFBundleShortVersionString: $(MARKETING_VERSION)
        CFBundleVersion: $(CURRENT_PROJECT_VERSION)
        NSExtension:
          NSExtensionAttributes:
            WKAppBundleIdentifier: com.fabienlopes.biotrack
          NSExtensionPointIdentifier: com.apple.widgetkit-extension
    entitlements:
      path: BioTrackWidgetExtension/BioTrackWidgetExtension.entitlements
      properties:
        com.apple.security.application-groups:
          - group.com.fabienlopes.biotrack
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.fabienlopes.biotrack.widget
        CODE_SIGN_STYLE: Automatic
        DEVELOPMENT_TEAM: X4Z4PH88ZT
        CURRENT_PROJECT_VERSION: 14
        MARKETING_VERSION: 1.2.4
        TARGETED_DEVICE_FAMILY: "1"
    dependencies:
      - sdk: WidgetKit.framework
      - sdk: AppIntents.framework
      - sdk: ActivityKit.framework
  BioTrackTests:
    type: bundle.unit-test
    platform: iOS
    deploymentTarget: "15.0"
    sources:
      - path: BioTrackTests
    dependencies:
      - target: BioTrack
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.fabienlopes.biotrack.tests
        GENERATE_INFOPLIST_FILE: YES
schemes:
  BioTrack:
    build:
      targets:
        BioTrack: all
        BioTrackWidgetExtension: all
        BioTrackTests: [test]
    run:
      config: Debug
    test:
      config: Debug
      gatherCoverageData: true
      targets:
        - BioTrackTests
